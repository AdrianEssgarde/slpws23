
- user_item_rel.each do |item|
    p.item_titles = item["name"]
    form action="/item/#{item["item_id"]}/delete" method="POST"
        input type="submit" value="Delete!"
    a.update_link href="/item/#{item["item_id"]}/edit" Update!